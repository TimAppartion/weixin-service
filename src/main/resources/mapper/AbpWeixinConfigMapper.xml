<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jiuzhou.user.mapper.AbpWeixinConfigMapper">
    <select id="getIdByTenantId" resultType="java.lang.Integer">
        select id from AbpOperatorsCompany where TenantId=#{Id} and IsDeleted=0
    </select>
</mapper>
