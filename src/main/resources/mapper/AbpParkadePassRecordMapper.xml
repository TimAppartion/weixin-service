<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jiuzhou.user.mapper.AbpParkadePassRecordMapper">

    <select id="getByPassageId" resultType="com.example.jiuzhou.user.view.ParkadePassView">
        select
            ppr.PassageId as passageId,
            ppr.PlateNumber as plateNumber,
            pp.parkId as parkId
        from AbpParkadePassRecord ppr
        left join AbpParkadePassage pp on ppr.PassageId = pp.Id
        where ppr.PassageId = #{passageId} and pp.parkId = #{parkId}
    </select>
</mapper>
