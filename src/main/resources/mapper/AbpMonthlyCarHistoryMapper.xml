<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jiuzhou.user.mapper.AbpMonthlyCarHistoryMapper">

    <insert id="insertOne">
        INSERT INTO AbpMonthlyCarHistory
        ( MonthlyCarId,Money,ParkIds,BeginTime,EndTime,Status,CreationTime,CreatorUserId,MonthyType,PayStatus,tranid )
        VALUES
        ( #{query.MonthlyCarId},#{query.Money},#{query.ParkIds},#{query.BeginTime},#{query.EndTime},#{query.Status},#{query.CreationTime},#{query.CreatorUserId},#{query.MonthyType},#{query.PayStatus},#{query.tranid} )
    </insert>
</mapper>
